import { Routes } from '@angular/router';
import { BanktableComponent } from './banktable/banktable.component';

export const routes: Routes = [
    {path:'banktable',component:BanktableComponent}
];
